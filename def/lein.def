if [[ "$IMAGE" = "" ]]; then
    IMAGE="clojure:lein-2.5.0"
fi
if [[ "$INTERACTIVE_TTY" = "" ]]; then
    INTERACTIVE_TTY=""
fi
if [[ "$OPTION" = "" ]]; then
    OPTION=""
fi
if [[ "$COMMAND" = "" ]]; then
    COMMAND=""
fi
