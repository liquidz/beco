if [[ "$IMAGE" = "" ]]; then
    IMAGE="elasticsearch:1"
fi
if [[ "$INTERACTIVE_TTY" = "" ]]; then
    INTERACTIVE_TTY=""
fi
if [[ "$PUBLISH" = "" ]]; then
    PUBLISH="-p 9200:9200 "
fi
if [[ "$COMMAND" = "" ]]; then
    COMMAND=""
fi
