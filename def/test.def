if [[ "$IMAGE" = "" ]]; then
    IMAGE="test/image:latest"
fi
if [[ "$INTERACTIVE_TTY" = "" ]]; then
    INTERACTIVE_TTY=""
fi
if [[ "$OPTION" = "" ]]; then
    OPTION=""
fi
if [[ "$COMMAND" = "" ]]; then
    COMMAND=""
fi
