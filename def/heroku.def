if [[ "$IMAGE" = "" ]]; then
    IMAGE="beco/heroku"
fi
if [[ "$INTERACTIVE_TTY" = "" ]]; then
    INTERACTIVE_TTY=""
fi
if [[ "$PUBLISH" = "" ]]; then
    PUBLISH=""
fi
if [[ "$COMMAND" = "" ]]; then
    COMMAND=""
fi
