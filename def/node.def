if [[ "$IMAGE" = "" ]]; then
    IMAGE="node:0.10"
fi
if [[ "$INTERACTIVE_TTY" = "" ]]; then
    INTERACTIVE_TTY=""
fi
if [[ "$PUBLISH" = "" ]]; then
    PUBLISH=""
fi
if [[ "$COMMAND" = "" ]]; then
    COMMAND=""
fi
