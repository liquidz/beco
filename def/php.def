if [[ "$IMAGE" = "" ]]; then
    IMAGE="php:5.6"
fi
if [[ "$INTERACTIVE_TTY" = "" ]]; then
    INTERACTIVE_TTY=""
fi
if [[ "$OPTION" = "" ]]; then
    OPTION=""
fi
if [[ "$COMMAND" = "" ]]; then
    COMMAND=""
fi
