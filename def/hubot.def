if [[ "$IMAGE" = "" ]]; then
    IMAGE="uochan/hubot"
fi
if [[ "$INTERACTIVE_TTY" = "" ]]; then
    INTERACTIVE_TTY=""
fi
if [[ "$OPTION" = "" ]]; then
    OPTION=""
fi
if [[ "$COMMAND" = "" ]]; then
    COMMAND="redis-server"
fi
