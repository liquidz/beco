if [[ "$IMAGE" = "" ]]; then
    IMAGE="uochan/ansible"
fi
if [[ "$INTERACTIVE_TTY" = "" ]]; then
    INTERACTIVE_TTY=""
fi
if [[ "$PUBLISH" = "" ]]; then
    PUBLISH=""
fi
if [[ "$COMMAND" = "" ]]; then
    COMMAND=""
fi
