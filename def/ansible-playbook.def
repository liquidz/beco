if [[ "$IMAGE" = "" ]]; then
    IMAGE="uochan/ansible"
fi
if [[ "$INTERACTIVE_TTY" = "" ]]; then
    INTERACTIVE_TTY=""
fi
if [[ "$PUBLISH" = "" ]]; then
    PUBLISH=""
fi
if [[ "$COMMAND" = "" ]]; then
    COMMAND=""
fi
if [[ "$NO_COMMAND" = "" ]]; then
    NO_COMMAND=""
fi
if [[ "$MOUNT_POSITION" = "" ]]; then
    MOUNT_POSITION=""
fi
