if [[ "$IMAGE" = "" ]]; then
    IMAGE="$(image)"
fi
if [[ "$INTERACTIVE_TTY" = "" ]]; then
    INTERACTIVE_TTY="@(if interactive-tty)-it@(end)"
fi
if [[ "$OPTION" = "" ]]; then
    OPTION="$(option)"
fi
if [[ "$COMMAND" = "" ]]; then
    COMMAND="$(command)"
fi
